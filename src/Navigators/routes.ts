const routes = {
  home: {
    path: '/',
  },
  auth: {
    path: '/auth',
    login: {
      path: '/auth/login',
    },
    register: {
      path: '/auth/register',
    },
  },
}

export default routes
